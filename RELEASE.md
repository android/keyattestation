# Release Notes

## v0.1
A test release.

It is a working version of the library at a given point in time, before any
BETA/1.0 release.

We plan to export more parameters and accept more checks but verification is
supported and the information for the minimum verification use case is
returned.